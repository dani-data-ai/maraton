{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install && npx prisma generate && cd ../app && npm install --include=dev && npx vite build --force"
  },
  "deploy": {
    "startCommand": "cd server && npx prisma db push --accept-data-loss && node seed.js && node index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
